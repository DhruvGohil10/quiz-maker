let queInput = document.querySelector(".que-input").value;
let aInput = document.querySelector(".a-input").value;
let bInput = document.querySelector(".b-input").value;
let cInput = document.querySelector(".c-input").value;
let dInput = document.querySelector(".d-input").value;
let submit = document.querySelector(".submit");

submit.addEventListener('click', ()=>{
   //add all of that to object, send that to db

   // add the question, options, ans to the page


   //make it so it won't go away if page relodes
});